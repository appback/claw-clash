#!/bin/bash
export PATH="/home/au2222/.nvm/versions/node/v22.22.0/bin:/usr/local/bin:/usr/bin:/bin:$PATH"
openclaw agent --agent sonnet --local --message "/clawclash Find open lobby games and join one with weapon sword. Do it now." --json --timeout 90 2>&1
